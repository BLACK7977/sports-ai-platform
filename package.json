{
  "name": "sports-ai-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "dev:offline": "set ENABLE_OFFLINE_MODE=true & next dev",
    "dev:cloud": "set ENABLE_OFFLINE_MODE=false & next dev",
    "db:migrate": "powershell -NoProfile -Command \"if (Test-Path .env.local) { $x = Get-Content .env.local | Select-String 'SUPABASE_SERVICE_ROLE_KEY'; if ($x) { Write-Host 'INFO: SUPABASE_SERVICE_ROLE_KEY detectado. Usa tu herramienta de Supabase preferida para correr migraciones en supabase/migrations/' } else { Write-Host 'MODO OFFLINE: sin SUPABASE_SERVICE_ROLE_KEY en .env.local. Migraciones saltadas (usa in-memory store).' } } else { Write-Host 'MODO OFFLINE: sin .env.local. Migraciones saltadas (usa in-memory store).' }\"",
    "db:seed": "powershell -NoProfile -Command \"if (Test-Path .env.local) { $x = Get-Content .env.local | Select-String 'SUPABASE_SERVICE_ROLE_KEY'; if ($x) { Write-Host 'INFO: SUPABASE_SERVICE_ROLE_KEY detectado. Usa tu herramienta de Supabase preferida para correr seed en supabase/seed/demo-soccer.sql' } else { Write-Host 'MODO OFFLINE: seed ya cargado en in-memory store al iniciar el proceso.' } } else { Write-Host 'MODO OFFLINE: seed ya cargado en in-memory store al iniciar el proceso.' }\"",
    "smoke": "tsx src/lib/scripts/smoke-tests.ts",
    "smoke:b3": "tsx src/lib/scripts/smoke-tests-bloque3.ts",
    "smoke:b4": "tsx src/lib/scripts/smoke-tests-bloque4.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "next": "16.3.4",
    "openai": "^7.15.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "server-only": "^0.0.1",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.3.4",
    "tailwindcss": "^4",
    "tsx": "^4.23.13",
    "typescript": "^5"
  }
}
